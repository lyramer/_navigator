{
  "name": "mapv0",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@trendmicro/react-sidenav": "^0.4.5",
    "bootstrap": "^4.1.3",
    "express": "^4.17.1",
    "leaflet": "^1.3.4",
    "react": "^16.5.2",
    "react-calendar": "^2.17.5",
    "react-dom": "^16.5.2",
    "react-leaflet": "^2.0.1",
    "react-popup": "^0.9.3",
    "react-router-dom": "^5.0.0",
    "react-scripts": "1.1.5",
    "react-sizes": "^2.0.0",
    "react-tiny-spin": "^0.6.5",
    "reactstrap": "^6.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "echo Hey!!! Please use npm run local-build if you want to build locally, or use npm run server-build if you want to deploy on the server",
    "local-build": "react-scripts build && echo 'if you are running this on the server, use npm run server-build to build the symlinks necessary to see the data'",
    "server-build": "react-scripts build && chmod +x ./symlink.sh && ./symlink.sh",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:5000/"
}
